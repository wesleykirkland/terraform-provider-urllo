# Local CLI configs (contain machine-specific paths).
dev.tfrc
mirror.tfrc

# Repo-local provider mirror (see build-local.sh TF_PLUGIN_MIRROR).
.terraform-mirror/

# Terraform working files.
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.*
crash.log
crash.*.log
*.tfvars
!*.tfvars.example
